#pragma once

/**
 * @brief Вычисляет значение функции cos(x)^3 в разложении в ряд Тейлора с заданной
 * точностью.
 *
 * @param x Аргумент функции
 * @return Значение функци в разложении в ряд Тейлора
 */
double get_taylor_cos_cubed(double &x);